#version 330 core

in vec2 UV;
in vec4 ShadowCoord;

out vec3 colour;

uniform sampler2D textureID;
uniform sampler2D depthTexture;

layout(location = 0) out vec3 color;

void main(){
    colour = texture(depthTexture, UV).r*texture(textureID, UV).rgb;
}